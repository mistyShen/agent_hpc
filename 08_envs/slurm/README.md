# Snakemake Slurm Profile Placeholder

This profile is a lightweight placeholder for later formal HPC execution.

- Intended root: `/shared/shen/cpu_ai_drug_design`
- Intended environment: `aml_rna`
- Current scope: dry-run planning and low-risk scaffold execution only

Example future usage:

```bash
python run_workflow.py --profile 08_envs/slurm --dry-run
```
