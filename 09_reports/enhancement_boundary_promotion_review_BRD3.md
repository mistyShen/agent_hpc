# Enhancement Boundary Promotion Review: BRD3 I-BET762 Surfaces

- Generated at: `2026-04-28`
- Scope: enhancement-line boundary review only.
- Decision target: whether current evidence supports moving beyond the older `BRD4-only material gain` reading.
- Boundary: this review does not update the formal rollout boundary, handoff package, frozen manuscript claims, or frozen benchmark policy.

## Review Question

Can the enhancement-line evidence be promoted from:

- `BRD4-only material gain`

to:

- `BET-family bounded material gain`

## Current Formal Boundary

The approved handoff state still uses the restrained headline:

- `weak_outside_BRD4__material_on_BRD4_IBET_focused_panels`

Its formal material-on-panel set remains:

- `BRD4_BD1_LIT002_V3EXP`
- `BRD4_BD1_LIT003_V3EXP`

This review does not change that formal state.

## New Positive Evidence

### BRD3 BD1 / 24OS

`BRD3_BD1_LIT002` is a non-BRD4, BET-family, I-BET762-focused validation surface using `BRD4_LIT_FOCUSED_002`.

| Case | Shortlist count | Known active shortlisted | Known active rank | Best background | Gap |
| --- | --- | --- | --- | --- | --- |
| `BRD3_BD1_LIT002` | `1` | `True` | `1` | `BG_BIPHENYL` | `1.0` |
| `BRD3_BD1_LIT002_V3EXP` | `1` | `True` | `1` | `BG_BENZAMIDE` | `6.559` |

Interpretation: this is a shortlist-preserving material signal on a non-BRD4 BET-family I-BET762-focused surface.

### BRD3 BD2 / 3S92

`BRD3_BD2_LIT002` is a second non-BRD4, BET-family, I-BET762-focused validation surface using the same focused panel.

| Case | Shortlist count | Known active shortlisted | Known active rank | Best background | Gap |
| --- | --- | --- | --- | --- | --- |
| `BRD3_BD2_LIT002` | `1` | `True` | `1` | `BG_BIPHENYL` | `2.520` |
| `BRD3_BD2_LIT002_V3EXP` | `1` | `True` | `1` | `BG_BENZAMIDE` | `7.459` |

Interpretation: this is also a shortlist-preserving material signal. It reduces the risk that `BRD3_BD1_24OS` was only a single-structure positive result.

## Counter-Evidence And Risks

- `BRD2_BD1_LIT002_V3EXP` remains a filtering-gated challenge: reranking improves ordering, but the known active does not enter the shortlist.
- `BRDT_BD1_LIT002_V3EXP` was also observed as a filter-gated challenge in the last-jump search, so not every non-BRD4 BET-family I-BET762-focused surface is rollout-safe.
- The new positive non-BRD4 material evidence is concentrated in `BRD3`, not yet distributed across multiple non-BRD4 BET members.
- A full `BET-family bounded material gain` headline would currently overstate the evidence because BRD2 and BRDT I-BET762-focused attempts have not provided rollout-safe material support.

## Review Decision

Current evidence supports retiring the internal `BRD4-only material gain` reading.

Current evidence does not yet support a formal promotion to broad `BET-family bounded material gain`.

Recommended interim headline for review use:

- `bounded material gain supported in BRD4 and BRD3 I-BET762-focused surfaces`

This wording is stronger than the old formal boundary, but still narrower than a full BET-family claim.

## Recommendation

- Do not update the formal rollout boundary directly from this report without explicit approval.
- If approved, update the formal boundary to the narrower interim headline above, not to full `BET-family bounded material gain`.
- Keep `BRD2_BD1_LIT002_V3EXP` and `BRDT_BD1_LIT002_V3EXP` outside rollout-safe material evidence.
- Continue treating v3 and case-aware gating as enhancement-line empirical policy only.
- For the next evidence step, prefer one additional non-BRD3, non-BRD4, I-BET762-focused surface with strong shortlist-preserving likelihood.

## Bottom Line

The enhancement line has crossed the old `BRD4-only` internal interpretation. It has not yet earned a full BET-family headline. The right next formal boundary, if approved, is a controlled intermediate boundary: material gain is currently supported in `BRD4` and `BRD3` I-BET762-focused surfaces.
